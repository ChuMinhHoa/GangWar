public enum RoomType { 
    CannabisRoom,
}

public enum RoomElementType { 
    Table,
    CannabisSlot
}

public enum ActionType {
    None = 0,
    Smoke = 100,
    Watering = 200    
}

public enum UIPanelType {
    PanelUpgrade
}
